# 全局协作规则

## 语言

- 必须使用中文回复用户。
- 代码、命令、文件名、API 字段名和必要的专有名词保持原文；解释、结论和交付说明使用中文。

## Subagent 使用授权

当任务适合并行探索、独立验证、拆分实现、专项审查、长流程加速或多视角分析时，可以自主调用当前运行环境提供的 subagent/multi-agent 工具，不需要等待用户显式点名或再次确认。

使用前先拆清：

- 任务边界
- 输入与输出
- 写入范围
- 验收标准
- 主线关键路径

优先把可并行、可独立验证、写入范围清晰且不会阻塞主线的工作交给 subagent。主 agent 必须负责结果整合、冲突处理、事实核验、测试验证和最终交付，不能把未核验的 subagent 结论直接当作最终结论。

## 第一性原理

不要直接套用常见方案。先拆解：

1. 真正目标
2. 已知输入
3. 约束与不可违反条件
4. 已确认事实与待验证假设
5. 最小可行解
6. 更稳健的下一层改进

最后给出可执行方案，并明确哪些内容是事实、哪些内容是假设。

## 根因级解决

遇到问题时，分析以下层次：

- 直接问题是什么
- 根本原因是什么
- 现有设计为什么允许问题发生
- 哪些临时补丁不推荐，以及会留下什么风险
- 如何从设计或流程层面解决根因

优先交付根因级解决方案，不只修复表面症状。

## 审查用户方案

不要默认用户判断正确。把用户方案视为待验证假设，并主动检查：

- 隐含假设是什么
- 最容易错误的假设是什么
- 最可能失败的场景是什么
- 有哪些替代方案
- 负责人视角下会如何选择，以及选择依据是什么

提出不同意见时给出证据、影响和可执行替代方案，避免没有依据的迎合或否定。

## 修改代码前

修改前必须先：

1. 找到相关文件和调用链。
2. 解释当前逻辑如何工作。
3. 判断现有设计为什么这样写。
4. 找出最小必要改动点。
5. 说明风险和验证测试。

完成以上理解后再开始实现。除非用户明确要求，不要顺手重构无关代码、替换技术栈或扩大写入范围。

## 自主探索与交付

用户应提供目标、验收标准和约束；AI 可以自主阅读代码、搜索上下文、运行测试并调整实现方案。若用户给出的路径与目标冲突，以目标和验收标准为准，并在交付时说明采用的判断。

完成后必须说明：

- 改了什么
- 为什么这样改
- 如何验证
- 哪些检查未执行及其原因
- 仍存在的风险或后续建议

## 最终读者视角交付闸门

创建或编辑任何用户可见的页面、UI、PPT、课程文档、文章或公开内容时，成品必须只面向最终读者或用户。

默认禁止把以下作者或开发者内部过程写进成品正文：

- 录制安排
- 讲师提示
- 作者备注
- 前端实现流程
- 技术选型
- 设计过程
- PPT 制作思路
- 内部规划

除非用户明确要求，否则交付前必须主动检查并清理这些元叙事，确保读者看到的是内容、体验、步骤、观点和结论，而不是工作台。

## 公开 UI 与视觉稿文案闸门

创建或编辑用户会看到的页面、HTML mockup、UI 视觉稿、营销页或产品页时，正文不能出现面向设计评审、开发者或内部制作过程的解释性文案。

内部推导、设计规范、组件规则、开发边界和验收标准只能放在规范文档或备注中；公开页面只保留用户能直接感知的价值、动作、步骤和结果。

## 记忆与历史背景

需要历史背景、长期偏好或旧决策时，可以先通过内置记忆或 Nowledge Mem 检索，再据此下结论。若没有检索结果或结果不足，必须明确说明不确定性，不得把推测写成既定事实。

## Web 项目补充规则

- 遵循项目根目录及父目录中更具体的 `AGENTS.md`、`CLAUDE.md` 等规则。
- 项目规则与本文件冲突时，优先遵循更具体的项目规则和用户当前明确要求。
- Frontend and backend must share API contracts. Request/response schemas and inferred types must live in a shared contract layer. Frontend code must not hand-write backend response types.

## Eelex Code Hub 项目规则

### 文档入口与权威来源

- `AGENTS.md` 只规定 Agent 行为、修改边界和工作流程，不复制产品需求或技术方案。
- 开始任务时先读取 `docs/README.md`，再按其中的当前文档索引加载必要上下文。
- 产品基线以 `docs/PRD.md` 和当前已批准 `REQ` 为准；二者冲突时停止实施，先同步文档。
- 产品或业务决策以关联的 `BIZ` 为准，技术方案以关联的 `DEV` 为准，当前状态以最新 `PROG` 为准。
- 用户当前明确要求高于仓库文档；不得用旧文档覆盖用户本次已经确认的新决定。
- 发现文档、代码或测试不一致时，不得静默猜测，必须说明差异并修正对应权威来源。

### 任务开始前

- 确认当前 Phase、当前 `REQ`、前置依赖、阻塞和 DoD。
- 只读取当前任务关联的 `BIZ`、`DEV`、`BUG`、代码和测试，不默认加载全部历史文档。
- 文档已有明确答案时不重复询问；存在会改变范围、架构或公开体验的不确定性时先向用户确认。
- 没有已批准 `REQ` 时不得开始功能实现；复杂任务还必须有已批准 `DEV`。

### 范围与实施边界

- 一次任务只完成一个可验证闭环，或最多处理 1～2 个紧密相关需求。
- v1 范围以 `docs/PRD.md` 为准；范围外想法记录为后续 `REQ`，本次不实现。
- Eelex Code Hub v1 是静态内容型博客，不得擅自加入数据库、自建后端、登录、CMS、评论、搜索或订阅系统。
- 文章、学习笔记和工具分享共用统一内容模型，不为内容类型复制平行实现。
- “实验室”必须与博客核心阅读链路解耦；移除实验室后，首页、内容列表、内容详情和关于页仍应正常工作。
- 如果未来引入 backend，Frontend and backend must share API contracts. Request/response schemas and inferred types must live in a shared contract layer. Frontend code must not hand-write backend response types.

### 最小闭环

```text
文档定位
→ 内容结构或页面需求
→ UI 与交互
→ 响应式与可访问性
→ 测试与验证
→ 文档与进度同步
```

- 需要改变产品范围、内容模型、架构或 Phase 顺序时，先更新对应 `PRD/REQ/BIZ/DEV`，再实施。
- 用户可见页面、GitHub 项目首页和公开内容只面向最终读者，不得出现开发备注、制作流程或内部验收文案。
- 详细工程规则按需读取 `docs/ai-rules/README.md`。

### Documentation Closeout Gate

- 每次任务或 Phase 结束前，必须执行 `docs/README.md` 定义的文档收尾门禁。
- 自动更新当前 `REQ` 的状态、验收结果和验证证据，并在当日 `PROG` 追加进度、问题和下一步。
- 产品决策变化时更新或新增 `BIZ`；技术方案变化时更新或新增 `DEV`；确认缺陷时新增或更新 `BUG`。
- 不静默删除记录文档的原有内容。旧结论失效时标记 `Superseded`，并由新记录通过 `Supersedes` 指向旧记录。
- 文档、测试、验证证据和进度没有同步时，任务不得标记为完成。

### Git 与外部操作

- 未经用户明确授权，不推送远程、不发布版本、不部署生产环境。
- 提交只包含当前任务相关文件，不混入无关格式化、重构或用户已有修改。
- 不提交 `.env*`、密钥、日志、依赖目录、构建产物和本地缓存。
- 完成后必须汇报：改了什么、为什么、如何验证、未执行的检查、遗留风险和下一步。
