# PROG-20260725：阅读画廊视觉刷新交付记录

## 文档信息

- 状态：Active
- 创建日期：2026-07-25
- 更新日期：2026-07-25
- 关联需求：[`REQ-20260725-02-visual-refresh.md`](REQ-20260725-02-visual-refresh.md)
- 关联决策：[`BIZ-20260725-03-visual-refresh.md`](BIZ-20260725-03-visual-refresh.md)
- 关联方案：[`DEV-20260725-02-visual-refresh.md`](DEV-20260725-02-visual-refresh.md)

## 已完成

- 已将用户确认的阅读画廊视觉迁移到正式站点页面：全局外框、首页、内容发现、内容卡片、详情页、关于页、404 和页脚。
- 首页与关于页的标题、简介统一为同一组排版，并增加同样的暖白半透明玻璃卡片。
- 已移除实验室导航、首页入口、`/lab`、阅读密度交互模块和专用测试；文章、学习笔记、工具分享、MDX、搜索/分类、详情目录、公开资料和 GitHub 链接保持原样。
- 用户已在本地预览确认 UI 效果符合预期。

## DoD 与验证证据

| DoD | 结果 | 证据 |
| --- | --- | --- |
| 内容发现与内容链路保留 | 通过 | `tests/page-shells.test.tsx` 与 3 个静态内容详情路由均通过构建验证。 |
| 实验室完整移除 | 通过 | 路由树不含 `/lab`；对应页面、状态模块和测试已删除。 |
| 视觉与页面一致性 | 通过 | 用户确认本地预览中的导航、首页、关于页和玻璃卡片效果。 |
| 自动质量门禁 | 通过 | `corepack pnpm check`：9 个测试文件、23 项测试、格式、类型和生产构建通过。 |
| Git 差异卫生 | 通过 | `git diff --check` 通过。 |

## 已知事项

- ESLint 仍报告 `src/content/repository.ts` 的 4 条既有未使用参数 warning；本次未修改该模块，且没有 lint error。
- Next.js 在 linked worktree 中提示检测到多个 lockfile 并推断 workspace root；生产构建成功，不影响本次输出。
- 尚未推送、部署、创建 PR 或核对远程 CI；线上 Netlify 仍是旧 UI。

## 下一步

1. 将 `codex/visual-refresh` 的独立提交安全整合到包含历史未提交修改的 `main`，逐个处理重叠文件。
2. 用户手动在 Netlify 发起部署，并提供实际 URL 后核对线上字体、路由、响应式与可访问性。

## 发布前更正

- 线上核对发现首页和关于页已更新，但 `/lab` 仍返回旧页面、`/robots.txt` 返回 404。根因是 Phase 6 源码未进入 `main`，且 Netlify 未从包含实验室移除的完整源状态重新构建。
- 已将 Phase 6 的 metadata、canonical、`robots.txt`、`sitemap.xml` 与 MDX 图片校验补整到 `main`；sitemap 不含已取消的 `/lab`。补整后 `corepack pnpm check` 通过 11 个测试文件、30 项测试与生产构建，构建路由已包含 `/robots.txt`、`/sitemap.xml` 且不含 `/lab`。
- 下一步：推送本次补整提交后，请用户在 Netlify 重新部署；部署完成后复核首页、关于页、`/lab`、`/robots.txt` 和 `/sitemap.xml`。

## 静态发布修复

- 线上发布后确认 `/lab` 仍返回旧实验室页面；带查询参数访问与清除 Netlify 构建缓存后仍可稳定复现，而本地提交 `5d0693c` 的生产路由树和构建产物均不含 `/lab`。已登记为 `BUG-20260725-01`。
- 用户批准静态导出方案后，新增 `output: "export"`、`netlify.toml` 和 metadata 路由静态声明；新增测试先在缺少这些配置时失败，补齐后通过。
- `corepack pnpm build` 已生成 `out`：包含首页、关于页、`robots.txt`、`sitemap.xml` 与 3 个内容页，不含任何 `lab` 文件。待完整质量门禁、提交与用户上传 `out` 后完成线上闭环。
