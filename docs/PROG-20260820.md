# PROG-20260820：参考博客体验改造进度

## 文档信息

- 状态：Completed
- 创建日期：2026-08-20
- 关联需求：[`REQ-20260820-01-reference-blog-experience.md`](REQ-20260820-01-reference-blog-experience.md)
- 关联决策：[`BIZ-20260820-01-reference-blog-scope.md`](BIZ-20260820-01-reference-blog-scope.md)
- 关联方案：[`DEV-20260820-01-reference-blog-implementation.md`](DEV-20260820-01-reference-blog-implementation.md)

## 已确认范围

- 采用参考站访客端结构，首页固定单列封面文章。
- 删除日历及日历跳转，但保留所有文章发布日期。
- 仅保留复制链接分享；删除分享海报。
- 商业正文模块与商店入口仅作为未来扩展，不在当前页面渲染。
- 保留项目/产品链接；反馈使用邮箱或公开联系方式。
- 删除评论、API 中转、正文购买导航、全屏透明壁纸和布局切换。

## 当前进度

- 已完成参考站桌面、关键状态和 390×844 移动端公开体验采集。
- 已读取现有 PRD、最近 REQ/BIZ/DEV/PROG、内容模型、页面调用链、测试与用户设计 token。
- 已创建本次批准的需求、决策与技术方案；应用实现与验证进行中。

## 验证证据

- 完成顶部导航、移动菜单、全局即时搜索、显示设置、桌面三栏首页、单列封面文章流、归档、搜索结果、RSS、文章阅读增强、项目入口和公开反馈入口。
- 所有文章展示发布日期；日历、日历跳转、网格/双列切换、评论、商店、API 中转、购买充值和分享海报均未出现在公开页面。
- 桌面 1265×712 与移动端 390×844 已按参考站截图执行两轮设计 QA；第一轮发现的重复欢迎卡与重复搜索入口已修正，第二轮无 P0/P1/P2 问题。证据见 [`../design-qa.md`](../design-qa.md)。
- 浏览器交互验证覆盖即时搜索、完整搜索、分类/标签筛选、归档、明暗主题、背景模式、移动菜单、复制链接、目录、上一篇/下一篇和返回顶部。
- 新浏览器页签复核：首页包含 3 个日期元素，无日历和布局切换，页面无横向溢出，控制台无 error 或 hydration 异常。
- `pnpm check` 通过：13 个测试文件、38 项测试、TypeScript、Prettier、ESLint 和 Next.js 静态构建成功；生成 12 个静态页面状态，包含 3 篇文章详情。
- 未执行推送、部署、远程 CI 或账号修改；这些操作不在本次授权范围。

## 遗留风险与下一步

- 4 条 ESLint warning 来自 `src/content/repository.ts` 的既有未使用解构变量，不阻塞构建；可在后续独立清理任务中处理。
- 未显式配置封面的文章复用默认阅读封面。新增内容时建议为每篇文章提供独立封面。
- R08/X02 商业能力仅保留在产品决策中，未来商业化时需建立新的 REQ/DEV 再实施。
- 下一项任务：由用户检查本地预览并提出删减或视觉调整；本轮不自动进入发布阶段。

## REQ-20260820-02 导航与弹层修正

- 用户确认导航两侧对齐、所有导航弹层互斥并支持空白点击关闭、归档与反馈重新分组、同步 GitHub 头像、公告不可关闭。
- 已只读确认当前根因：导航与内容使用不同的内边距基线；多个原生 `<details>` 无共享状态；公告内部保存可见性状态。
- 已从用户授权的浏览器读取 `https://github.com/Ee1ex` 当前公开头像地址；实现和验证进行中。

### 完成与验证

- 导航与首页三栏的左右边缘浏览器测量差值均为 `0px`。
- 项目、关于、显示和移动菜单改为共享受控状态；打开其他菜单、点击空白、聚焦搜索和 `Escape` 均会关闭旧弹层。
- 归档已收入项目菜单，反馈已收入关于菜单；移动菜单采用相同分组。
- GitHub 公开头像已保存为项目内 413×413 PNG 并用于首页、关于页；公告不再提供关闭入口。
- 桌面浏览器交互、390×844 移动端无溢出验证和控制台检查通过。
- `pnpm check` 通过：13 个测试文件、40 项测试、格式、类型、Lint 与 12 个静态页面构建成功；4 条既有 warning 不阻塞。
- 未删除旧头像，未推送、部署、发送消息或修改外部账号。

## REQ-20260820-03 横幅、图标与简介调整

- 用户确认个人简介改为“兴趣使然的个人开发者”，项目和关于弹层都向右展开，导航增加内容图标，并将上传四人照作为横幅壁纸。
- 已在参考站确认水波纹由四层 SVG `<use>` 构成，透明度逐层增加，循环周期约为 7、10、13、20 秒；不复制其标题和品牌内容。
- 已确认原横幅仍使用默认阅读封面和单层渐变，关于弹层单独使用 `right-0`；已按本次需求完成替换与统一。

### 完成与验证

- 个人简介已改为“兴趣使然的个人开发者”；桌面与移动导航使用统一语义线性图标。
- 项目与关于弹层浏览器实测均向右展开，左边缘与各自按钮差值为 `0px`；空白点击和菜单互斥行为继续有效。
- 用户上传四人照已保存为本地横幅资产；横幅底部采用四层 SVG 波纹，纯色模式隐藏横幅并恢复紧凑间距。
- 390×844 移动端保留四人主体且无横向溢出；桌面显示设置浮层层级已修正，控制台无运行时错误。
- `pnpm check` 通过：13 个测试文件、42 项测试、格式、类型、Lint 与 12 个静态页面构建成功；4 条既有 warning 不阻塞。
- 未删除文件，未推送、部署、发送消息或修改账号权限。

## GitHub 新版本发布授权

- 用户于 2026-08-20 明确授权暂存本轮确认的新版文件、创建版本提交并将 `main` 快进更新至该提交。
- 用户已知并接受更新 `main` 会触发现有 Netlify 自动生产部署。
- 发布前基线：`pnpm check` 通过，13 个测试文件、44 项测试、类型、格式、Lint 与 12 个静态页面构建成功；浏览器桌面/移动端验收完成。
- 提交范围排除本地 `.codex/` 与已无页面引用的 `public/eelex-avatar.png`；不使用强制推送。
- 远程提交、分支指针和 CI/部署状态以推送后的 GitHub 回读为准。

## REQ-20260820-05 静态滚动横幅与底部渐隐

- 用户确认取消流动波浪，只保留指定图片横幅与主页背景色渐隐，并要求横幅随页面滚动消失。
- 指定图片与当前项目横幅的 SHA-256 完全一致，无需重复覆盖资产。
- 只读确认当前固定行为来自 `position: fixed`；波浪由组件 SVG、普通关键帧和减少动效关键帧共同构成。实施与验证进行中。

### 完成与验证

- 横幅组件已移除全部 SVG 波浪结构，CSS 已移除波浪选择器及普通/减少动效关键帧。
- 横幅底部以主题画布色分阶段渐隐；亮色浏览器计算值最终为 `rgb(245, 245, 245)`，与主页画布一致。
- 横幅使用 `position: absolute`；滚动 636px 后横幅底部为 `-60px`，确认完全离开视口。
- 桌面与 390×844 浏览器无横向溢出，指定图片四人构图保持可见。
- `pnpm check` 通过：13 个测试文件、44 项测试、格式、类型、Lint 与 12 个静态页面构建成功；4 条既有 warning 不阻塞。
- 未删除文件，未推送、部署、发送消息或修改账号权限。

## REQ-20260820-04 横幅动效、裁切与 GitHub 官方图标修正

- 用户确认 GitHub 入口必须使用 GitHub.com 官方图标，网站默认纯色背景，并修正静态波浪与横幅深度、裁切。
- 只读复现确认：默认和恢复默认已是 `solid`；波浪静止由减少动效规则强制停止；当前横幅高度 26rem，明显小于参考站约 565px 的波浪底部位置。
- 参考站当前四层波浪为 7、10、13、20 秒无限循环，波浪区域高度约 130px；实施与验证进行中。

### 完成与验证

- GitHub 项目入口已改用 GitHub 官方 Invertocat 几何路径；不依赖外部 CDN，颜色继承当前主题。
- 无保存偏好和恢复默认继续使用纯色背景；交付浏览器预览已恢复为纯色。
- 新 1200×521 横幅资产已替换旧图；桌面四张脸可见，390×844 保留四人且无横向溢出。
- 横幅底部实测 576px、波浪区域 130px；四层在减少动效环境放慢但保持无限循环，700ms 两次采样均检测到位移。
- `pnpm check` 通过：13 个测试文件、44 项测试、格式、类型、Lint 与 12 个静态页面构建成功；4 条既有 warning 不阻塞。
- 未删除文件，未推送、部署、发送消息或修改账号权限。

## REQ-20260820-06 Eelex Blog 品牌改名

- 用户批准将正式展示名统一为 `Eelex Blog`、目标仓库名统一为 `Ee1ex/eelex-blog`、npm 包名统一为 `eelex-blog`。
- 实施只覆盖站点品牌、SEO、页面壳、包名、README 和治理记录；不改变视觉、路由、内容架构、域名或部署。
- 已在 `codex/eelex-blog-rename` 隔离分支完成品牌合同与最小实现；原工作树的 `.codex/` 和 `public/eelex-avatar.png` 不进入本分支。
- 关联需求：[`REQ-20260820-06-eelex-blog-rename.md`](REQ-20260820-06-eelex-blog-rename.md)。
- 关联方案：[`DEV-20260820-03-eelex-blog-rename.md`](DEV-20260820-03-eelex-blog-rename.md)。
- 定向验证和完整质量门禁结果将在 DEV readiness 中记录；远程仓库改名、Push、PR 和部署均未执行。
