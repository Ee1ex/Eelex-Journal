# PROG-20260823：移除初始示范文章

## 文档信息

- 状态：Completed
- 创建日期：2026-08-23
- 关联需求：[`REQ-20260823-01-remove-demo-articles.md`](REQ-20260823-01-remove-demo-articles.md)
- 关联产品：[`PRD.md`](PRD.md)

## 已确认范围

- 删除 3 篇最初始的示范文章。
- 只保留 ELX Level 与 ELX-CNCOLOR 两篇正式文章。
- 保留所有图片、历史记录、页面结构和内容模型。
- 本地验证与提交完成后，远程 Push 仍须执行前确认。

## 完成内容

- 从 GitHub `main@bef6d3a` 创建 `codex/remove-demo-articles` 隔离分支。
- 删除 `designing-readable-interfaces.mdx`、`weekly-learning-notes-01.mdx` 与 `spacing-scale-checklist.mdx`。
- 将首页测试改为断言两个保留 slug，并明确排除 3 个旧 slug。
- 将详情 metadata 与 sitemap 测试切换到 ELX-CNCOLOR 和 ELX Level。
- 对既有未格式化的 ELX-CNCOLOR 表格与 JSON 空格执行 Prettier 机械规范化；公开内容语义不变。

## 验证证据

- 修改前定向测试：2 个测试文件、6 项测试中 5 项通过；唯一失败是既有 sitemap 期望值漏掉 ELX-CNCOLOR。
- 修改后定向测试：2 个测试文件、6 项测试全部通过。
- `corepack pnpm check` 通过：13 个测试文件、46 项测试、Prettier、类型、ESLint 与 Next.js 静态构建成功。
- ESLint 保留 `src/content/repository.ts` 的 4 条既有未使用变量 warning，无 error。
- 构建仅生成 `/content/elx-cncolor` 与 `/content/elx-level-project-workflow` 两个详情页；3 个旧 slug 在 `out/` 中无命中。
- RSS 与 sitemap 均只包含两个保留 slug；`git diff --check` 通过。
- 原工作树的 `.codex/` 与 `public/eelex-avatar.png` 未进入隔离分支。

## 问题与处理

- 首次离线安装因缓存缺少 `@mdx-js/loader` 失败；按冻结 lockfile 补齐依赖后未产生依赖或 lockfile Diff。
- 首次完整门禁因 ELX-CNCOLOR 既有 Prettier 差异停止；机械格式化并审查 Diff 后完整门禁通过。

## 遗留风险与下一步

- 本地变更已准备为单一交付提交；尚未 Push、回读远端 `main`、核对 GitHub CI 或线上部署。
- Push 至 `main` 会触发现有 Netlify 自动生产部署；执行前必须再次获得用户确认。
